# Server Monitor Configuration
# Copy this to /opt/server-monitor/etc/config.env

# Discord webhook URL (REQUIRED)
DISCORD_WEBHOOK=""

# Server hostname
HOSTNAME=""

# Installation directory
INSTALL_DIR="/opt/server-monitor"

# Rate limiting (seconds between same alert type)
RATE_LIMIT_SECONDS=60

# Maximum alerts per minute (not currently enforced, for future use)
MAX_ALERTS_PER_MINUTE=10

# Log file for local backup
LOG_FILE="/opt/server-monitor/logs/monitor.log"
