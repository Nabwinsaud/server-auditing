# Server Monitor Configuration
# Copy this to /opt/server-monitor/etc/config.env
# SECURITY: chmod 600 this file to protect your webhook URL

# Discord webhook URL (REQUIRED)
# Get from: Discord Server → Settings → Integrations → Webhooks
DISCORD_WEBHOOK=""

# Server hostname (auto-detected if empty)
HOSTNAME=""

# Installation directory
INSTALL_DIR="/opt/server-monitor"

# Timezone for alert timestamps (default: UTC)
# Examples: Asia/Kathmandu, America/New_York, Europe/London, Asia/Kolkata
TIMEZONE="UTC"

# Rate limiting - minimum seconds between alerts of same type
# Prevents Discord spam. Recommended: 300 (5 min) for production
RATE_LIMIT_SECONDS=300

# Log file for local backup of all alerts
LOG_FILE="/opt/server-monitor/logs/monitor.log"
